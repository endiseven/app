name: CI/CD Pipeline

on:
  push:
    paths-ignore:
     # - 'app/.github/workflows/*'
     # - 'app/app/**' 
      - '**.md' 
      #- '**.yml'
      #- '**.py'
    branches:
      - main
  release:
    types: [created]

jobs:
  build-and-push:
     runs-on: ubuntu-22.04
     steps:
     - name: Login to Docker Hub
       uses: docker/login-action@v1
       with:
         username: ${{ secrets.DOCKERHUB_USER }}
         password: ${{ secrets.DOCKERHUB_TOKEN }}
          
     - uses: actions/checkout@v2
      
     - name: Set up Python
       uses: actions/setup-python@v2
       with:
         python-version: '3.x'
      
     - name: Build and push Docker image
       uses: docker/build-push-action@v2
       with:
         context: .
         push: true
         tags: endi7/fastapi001:v.1.0
         
  alterar_tag:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          repository: endiseven/gitops
          token: ${{ secrets.GIT_OPS }}

      - name: Modify yaml File
        run: |
          sed -i 's/main/v.1.0/g' ./fastapi/values.yaml

      - name: Commit and Push Changes
        run: |
          git config user.name "endiseven"
          git config user.email "endi.sete@gmail.com"
          git add ./README.md
          git commit -m "Update image name in md file"
          git push


